import { badRequest, serverError, forbidden, notFound, noContent, conflict } from './components/index'

export default {
  badRequest,
  serverError,
  notFound,
  forbidden,
  noContent,
  conflict
}